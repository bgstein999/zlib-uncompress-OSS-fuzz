A student project which uses OSS-Fuzz for the purpose of fuzzing the uncompress() function of zlib.

To use, clone a copy of https://github.com/google/oss-fuzz, and then replace the existing architecture for zlib with these files.
